# honeybee
a user-friendly conversion script for void linux.

# what does it do?

self explanatory as mentioned above, archinstall after-install but for void linux.

to be in more detail, it installs neccesary packages that are finnicky to figure out when erroring (ex libstdc++ which alacritty needs on older systems) that void does not have by default.
